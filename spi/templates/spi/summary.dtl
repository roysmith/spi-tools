<!DOCTYPE html>
<html>
<body>
  <h1>SPI Summary</h1>
  <h2>Input</h2>
  <form action="/spi/summary" method="post">
    {% csrf_token %}
    {{ form }}
    <input type="submit" value="OK">
  </form>
  {% if info %}
  <h2>Socks</h2>
  <ul>
    {% for sock in info %}
    <li>{{ sock.username }} {% if sock.blocked %} (blocked){% endif %}</li>
    {% endfor %}
  </ul>
  {% endif %}
</body>
</html>
